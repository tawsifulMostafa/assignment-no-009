import LoginForm from "@/Components/LoginForm/LoginForm";
 
export const metadata = {
    title: "Login",
}

const LoginPage = () => {
   
    return (
        <div>
          <LoginForm/>
        </div>
    );
};

export default LoginPage;